<% layout(layout_directory+'/mainLayout') -%>
<div class="kt-subheader   kt-grid__item" id="kt_subheader">
    <div class="kt-subheader__main">
        <h3 class="kt-subheader__title"> <%= page_title %> </h3>
        <span class="kt-subheader__separator kt-subheader__separator--v"></span>
        <div class="kt-subheader__breadcrumbs">
            <a href="<%= generateUrl('user.dashboard') %>" class="kt-subheader__breadcrumbs-home">Dashboard</a>
            <span class="kt-subheader__breadcrumbs-separator"></span>
            <span class="kt-subheader__breadcrumbs-link kt-subheader__breadcrumbs-link--active"><%= page_title %></span>
        </div>
    </div>
</div>

<div class="kt-portlet">
    <div class="kt-portlet__head">
        <div class="kt-portlet__head-label">
            <h3 class="kt-portlet__head-title"><%= page_title %></h3>
        </div>
    </div>
    <div class="kt-portlet__body">
        <form class="kt-form kt-form--label-right" id="editvideo"
            action="<%= generateUrl('admin.slider.updateslidervideo') %>" method="POST" enctype='multipart/form-data'>
            <div class="tab-content">
                <div class="form-group row">
                    <div class="col-lg-6">
                        <label>Slider Video:</label>
                        <input type="file" name="slider_video" id="slider_video" class="form-control imagepicker"
                            accept="video/*">
                    </div>
                    <div class="col-lg-6">
                        <%if(response.cms_data.slider_video == ''){%>
                        <label>No Video found</label>
                        <%} else {%>

                        <video autoplay="true" oncanplay="this.play()" width="400" alt="Video File Not Found"
                            onloadedmetadata="this.muted = true">
                            <source src="/uploads/slider/<%=response.cms_data.slider_video %>">
                        </video>
                        <%}%>
                    </div>
                    
                </div>
            </div>
            <!-- hidden user id -->
            <input type="hidden" name="id" value="<%=response.cms_data._id%>">

                        <div class="kt-portlet__foot">
                            <div class="kt-form__actions">
                                <div class="row">
                                    <div class="col-lg-6">
                                        <a href="<%= generateUrl('admin.slider.editslidervideo') %>"
                                            class="btn btn-secondary">Cancel</a>
                                        <button type="submit" class="btn btn-primary">Update</button>

                                    </div>
                                </div>
                            </div>
                        </div>
        </form>

    </div>
</div>